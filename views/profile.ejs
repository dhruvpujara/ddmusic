```html
<!DOCTYPE html>
<html lang="en">
<head>
    <!-- ...existing head content... -->
</head>
<body class="bg-gray-800 text-white">
    <!-- ...existing header and navigation... -->

    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold mb-4">Profile</h1>

        <div class="bg-gray-900/50 p-6 rounded-lg">
            <h2 class="text-xl font-semibold mb-4">Welcome, <%= username %>!</h2>

            <div class="stats grid grid-cols-2 gap-4 mt-6">
                <div class="stat bg-gray-900/50 p-4 rounded-lg text-center">
                    <span class="text-2xl font-bold text-violet-400"><%= likedSongsCount || 0 %></span>
                    <p class="text-gray-400 text-sm">Liked Songs</p>
                </div>
                <div class="stat bg-gray-900/50 p-4 rounded-lg text-center">
                    <span class="text-2xl font-bold text-violet-400"><%= typeof playlistCount !== 'undefined' ? playlistCount : 0 %></span>
                    <p class="text-gray-400 text-sm">Playlists</p>
                </div>
            </div>

            <!-- ...existing profile content... -->
        </div>
    </div>

    <!-- ...existing footer... -->
</body>
</html>
```