<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shared Playlists - DDMusic</title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* Ensure main content area creates proper stacking context */
main {
    position: relative;
    z-index: 1;
}

/* Specific container for playlists */
section.py-4:has(.playlist-card) {
    position: relative;
    z-index: 1;
}
        /* Modal styles */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.8);
            z-index: 100;
            display: none;
        }

        .modal-content {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: #1f2937;
            padding: 25px;
            border-radius: 12px;
            z-index: 101;
            display: none;
            width: 90%;
            max-width: 400px;
            border: 1px solid #4b5563;
        }

        /* Form styles */
        .form-input {
            background: #374151;
            border: 1px solid #4b5563;
            border-radius: 8px;
            padding: 12px 16px;
            color: white;
            width: 100%;
            margin-top: 5px;
            font-size: 16px;
        }

        .form-input:focus {
            outline: none;
            border-color: #8b5cf6;
            box-shadow: 0 0 0 2px rgba(139, 92, 246, 0.2);
        }

        .form-label {
            display: block;
            color: #d1d5db;
            margin-bottom: 8px;
            font-weight: 500;
        }

        .submit-btn {
            background: linear-gradient(135deg, #8b5cf6 0%, #6366f1 100%);
            color: white;
            padding: 12px 20px;
            border-radius: 8px;
            font-weight: 600;
            width: 100%;
            margin-top: 20px;
            transition: all 0.3s;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
        }

        .submit-btn:active {
            transform: translateY(0);
        }

        /* Number input specific */
        input[type="number"]::-webkit-inner-spin-button,
        input[type="number"]::-webkit-outer-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }

        /* Create button styles */
        .create-btn {
            cursor: pointer;
        }

        /* Join form specific */
        .join-form {
            display: flex;
            gap: 10px;
        }

        .join-input {
            flex: 1;
        }

        /* Code display styles */
        .share-code {
            font-family: 'Courier New', monospace;
            letter-spacing: 1px;
            font-weight: 600;
        }

        /* Playlist card hover effects */
.playlist-card {
    transition: transform 0.2s ease;
    position: relative;
    min-height: 120px;
    /* Add these lines: */
    isolation: isolate; /* Creates a new stacking context */
    z-index: 1; /* Establishes stacking context */
}

        .playlist-card:hover {
            transform: translateY(-2px);
        }

 

        .menu-icon:hover {
            background: rgba(0, 0, 0, 0.7);
            transform: scale(1.1);
        }

        .menu-icon i {
            color: white;
            font-size: 16px;
        }

/* Update the info-dropdown to work within the new stacking context */
.info-dropdown {
    position: absolute;
    top: 50px;
    right: 12px;
    background: rgba(0, 0, 0, 0.95);
    backdrop-filter: blur(10px);
    border-radius: 8px;
    padding: 12px 16px;
    display: none;
    flex-direction: column;
    gap: 12px;
    align-items: flex-start;
    z-index: 1000; /* Reduced but still high within its context */
    border: 1px solid rgba(255, 255, 255, 0.1);
    animation: slideDown 0.3s ease;
    min-width: 200px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
}

/* Also ensure the menu icon has proper stacking */
.menu-icon {
    position: absolute;
    top: 12px;
    right: 12px;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 20; /* Keep this higher than parent */
    transition: all 0.3s ease;
    backdrop-filter: blur(4px);
}

        .info-dropdown.show {
            display: flex;
        }

        .info-item {
            display: flex;
            align-items: center;
            gap: 10px;
            color: white;
            font-size: 14px;
            width: 100%;
            padding: 4px 0;
        }

        .info-item i {
            color: #8b5cf6;
            font-size: 12px;
            width: 20px;
            text-align: center;
        }

        .info-item .count {
            font-weight: 600;
            margin-left: 2px;
            font-size: 13px;
        }

        .info-item .label {
            color: #9ca3af;
            font-size: 12px;
            margin-left: auto;
        }

        /* Action buttons in dropdown */
        .dropdown-action {
            width: 100%;
            margin-top: 8px;
            padding-top: 8px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .action-btn {
            width: 100%;
            padding: 8px 12px;
            border-radius: 6px;
            font-weight: 500;
            font-size: 13px;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            margin-top: 4px;
        }

        .delete-btn {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            color: white;
        }

        .delete-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
        }

        .leave-btn {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
            color: white;
        }

        .leave-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(245, 158, 11, 0.3);
        }

        /* Confirmation modal for delete/leave */
        .delete-confirm-modal,
        .leave-confirm-modal {
            display: none;
        }

        .delete-confirm-modal.show,
        .leave-confirm-modal.show {
            display: block;
        }

        .modal-actions {
            display: flex;
            gap: 10px;
            margin-top: 20px;
        }

        .cancel-btn {
            background: #4b5563;
            color: white;
            padding: 10px 20px;
            border-radius: 6px;
            font-weight: 500;
            flex: 1;
            border: none;
            cursor: pointer;
        }

        .confirm-delete-btn {
            background: #ef4444;
            color: white;
            padding: 10px 20px;
            border-radius: 6px;
            font-weight: 500;
            flex: 1;
            border: none;
            cursor: pointer;
        }

        .confirm-leave-btn {
            background: #f59e0b;
            color: white;
            padding: 10px 20px;
            border-radius: 6px;
            font-weight: 500;
            flex: 1;
            border: none;
            cursor: pointer;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body class="bg-black min-h-screen flex flex-col">
    <!-- Header -->
    <header class="fixed top-0 w-full bg-black/90 backdrop-blur-lg z-50 px-4 py-3 border-b border-gray-800">
        <div class="flex justify-between items-center">
            <h1 class="text-xl font-bold text-white">Shared Playlists</h1>
            <button id="infoBtn" class="w-10 h-10 rounded-full bg-gray-800 hover:bg-gray-700 
                       flex items-center justify-center">
                <i class="fas fa-info text-gray-300"></i>
            </button>
        </div>
    </header>

    <!-- Info Modal -->
    <div id="infoModalOverlay" class="modal-overlay"></div>
    <div id="infoModal" class="modal-content">
        <div class="flex justify-between items-center mb-4">
            <h2 class="text-xl font-bold text-white">About Shared Playlists</h2>
            <button id="closeInfoModal" class="w-8 h-8 rounded-full bg-gray-800 hover:bg-gray-700 
                       flex items-center justify-center">
                <i class="fas fa-times text-gray-300"></i>
            </button>
        </div>
        <p class="text-white">
            Listen your favorite songs along with your friends. Just share the playlist code with them.
        </p>
    </div>

    <!-- Delete Confirmation Modal -->
    <div id="deleteConfirmModalOverlay" class="modal-overlay"></div>
    <div id="deleteConfirmModal" class="modal-content delete-confirm-modal">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-xl font-bold text-white">Delete Playlist</h2>
            <button id="closeDeleteModal" class="w-8 h-8 rounded-full bg-gray-800 hover:bg-gray-700 
                       flex items-center justify-center">
                <i class="fas fa-times text-gray-300"></i>
            </button>
        </div>
        <p class="text-white mb-4">Are you sure you want to delete this playlist? This action cannot be undone.</p>
        <div class="modal-actions">
            <button id="cancelDeleteBtn" class="cancel-btn">Cancel</button>
            <button id="confirmDeleteBtn" class="confirm-delete-btn">
                <i class="fas fa-trash mr-2"></i> Delete
            </button>
        </div>
    </div>

    <!-- Leave Confirmation Modal -->
    <div id="leaveConfirmModalOverlay" class="modal-overlay"></div>
    <div id="leaveConfirmModal" class="modal-content leave-confirm-modal">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-xl font-bold text-white">Leave Playlist</h2>
            <button id="closeLeaveModal" class="w-8 h-8 rounded-full bg-gray-800 hover:bg-gray-700 
                       flex items-center justify-center">
                <i class="fas fa-times text-gray-300"></i>
            </button>
        </div>
        <p class="text-white mb-4">Are you sure you want to leave this playlist? You won't be able to access it unless you rejoin with the code.</p>
        <div class="modal-actions">
            <button id="cancelLeaveBtn" class="cancel-btn">Cancel</button>
            <button id="confirmLeaveBtn" class="confirm-leave-btn">
                <i class="fas fa-sign-out-alt mr-2"></i> Leave
            </button>
        </div>
    </div>

    <!-- Create Playlist Modal -->
    <div id="createModalOverlay" class="modal-overlay"></div>
    <div id="createModal" class="modal-content">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-xl font-bold text-white">Create Shared Playlist</h2>
            <button id="closeCreateModal" class="w-8 h-8 rounded-full bg-gray-800 hover:bg-gray-700 
                       flex items-center justify-center">
                <i class="fas fa-times text-gray-300"></i>
            </button>
        </div>

        <form id="createPlaylistForm" action="/create/shared/playlist" method="POST">
            <div class="mb-4">
                <label for="playlistName" class="form-label">Playlist Name</label>
                <input type="text" id="playlistName" name="name" class="form-input" placeholder="Enter playlist name"
                    required maxlength="50">
                <p class="text-gray-400 text-xs mt-2">Maximum 50 characters</p>
            </div>

            <button type="submit" class="submit-btn">
                <i class="fas fa-plus-circle mr-2"></i> Create Playlist
            </button>
        </form>
    </div>

    <!-- Main Content -->
    <main class="flex-1 mt-16 mb-32 px-4">
        <% if (!isLoggedIn) { %>
            <div class="h-[80vh] flex flex-col items-center justify-center">
                <div class="text-center space-y-4">
                    <i class="fas fa-lock text-4xl text-gray-500"></i>
                    <h2 class="text-2xl font-bold text-white">Login Required</h2>
                    <p class="text-gray-400">Please login to access shared playlists</p>
                    <a href="/login"
                        class="inline-block bg-gradient-to-r from-purple-600 to-blue-600 px-8 py-3 rounded-lg text-white mt-4">
                        Login to Continue
                    </a>
                </div>
            </div>
        <% } else { %>
            <!-- Create Shared Playlist Button -->
            <section class="py-6">
                <div id="createPlaylistBtn" class="create-btn block">
                    <div
                        class="bg-gradient-to-r from-purple-600 to-blue-600 p-4 rounded-xl hover:opacity-90 transition-opacity">
                        <div class="flex items-center space-x-4">
                            <div class="w-16 h-16 bg-black/30 rounded-lg flex items-center justify-center">
                                <i class="fas fa-plus-circle text-2xl text-white"></i>
                            </div>
                            <div class="flex-1">
                                <h2 class="text-white text-lg font-bold">Create Shared Playlist</h2>
                                <p class="text-gray-200 text-sm mt-1">
                                    Start a new playlist and invite friends to collaborate
                                </p>
                            </div>
                            <i class="fas fa-chevron-right text-white/60 text-xl"></i>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Join Shared Playlist Section -->
            <section class="py-4">
                <div class="bg-gray-900/50 p-4 rounded-xl border border-gray-700">
                    <h2 class="text-white text-lg font-bold mb-3">Join Shared Playlist</h2>
                    <div class="space-y-3">
                        <form id="joinPlaylistForm" class="join-form">
                            <input type="number" id="playlistCode" placeholder="Enter 9-digit code"
                                class="form-input join-input" min="100000000" max="999999999" required
                                pattern="\d{9}" oninput="this.value=this.value.slice(0,9)">
                            <button type="submit"
                                class="bg-purple-600 px-6 py-3 rounded-lg text-white font-medium hover:bg-purple-700">
                                <i class="fas fa-sign-in-alt mr-2"></i> Join
                            </button>
                        </form>
                        <p class="text-gray-400 text-xs">
                            <i class="fas fa-info-circle mr-1"></i>
                            Enter 9-digit code shared by your friend
                        </p>
                    </div>
                </div>
            </section>

            <!-- Shared Playlists Section -->
            <section class="py-4">
                <div class="mb-3 flex justify-between items-center">
                    <h2 class="text-white text-lg font-semibold">Shared with You</h2>
                    <span class="text-gray-400 text-sm">
                        <i class="fas fa-list mr-1"></i>
                        <%= sharedPlaylistsCount || 0 %> playlists
                    </span>
                </div>

                <div class="space-y-4">
                    <% if (sharedPlaylists && sharedPlaylists.length > 0) { %>
                        <% sharedPlaylists.forEach((playlist, index)=> { %>
                            <div class="playlist-card bg-gradient-to-r from-purple-600 to-blue-600 p-4 rounded-xl">
                                <!-- Menu Icon - Moved to top right -->
                                <div class="menu-icon" data-playlist-index="<%= index %>">
                                    <i class="fas fa-ellipsis-v"></i>
                                </div>

                                <!-- Dropdown Info - Vertical layout -->
                                <div class="info-dropdown" id="info-dropdown-<%= index %>">
                                    <div class="info-item">
                                        <i class="fas fa-user-friends"></i>
                                        <span class="count">
                                            <%= playlist.members ? playlist.members.length : 1 %>
                                        </span>
                                        <span class="label">Members</span>
                                    </div>
                                    <div class="info-item">
                                        <i class="fas fa-music"></i>
                                        <span class="count">
                                            <%= playlist.songs ? playlist.songs.length : 0 %>
                                        </span>
                                        <span class="label">Songs</span>
                                    </div>
                                    <div class="info-item">
                                        <i class="fas fa-hashtag"></i>
                                        <span class="count share-code-dropdown"
                                            data-code="<%= playlist.shareCode || 'N/A' %>">
                                            <%= playlist.shareCode || 'N/A' %>
                                        </span>
                                        <span class="label">Code</span>
                                    </div>
                                    
                                    <!-- Action buttons - Only show one based on ownership -->
                                    <div class="dropdown-action">
                                        <% if (playlist.ownerId && ownerId && 
                                            playlist.ownerId.toString() === ownerId.toString()) { %>
                                            <!-- Owner sees delete button -->
                                            <form method="POST" 
                                                  action="/delete/<%= playlist._id %>/sharedPlaylist" 
                                                  id="delete-form-<%= playlist._id %>">
                                                <button type="button" 
                                                        class="action-btn delete-btn delete-playlist-btn"
                                                        data-playlist-id="<%= playlist._id %>"
                                                        data-playlist-name="<%= playlist.name || 'Playlist' %>">
                                                    <i class="fas fa-trash mr-2"></i> Delete Playlist
                                                </button>
                                            </form>
                                        <% } else { %>
                                            <!-- Member sees leave button -->
                                            <form method="POST" 
                                                  action="/leave/<%= playlist._id %>" 
                                                  id="leave-form-<%= playlist._id %>">
                                                <button type="button" 
                                                        class="action-btn leave-btn leave-playlist-btn"
                                                        data-playlist-id="<%= playlist._id %>"
                                                        data-playlist-name="<%= playlist.name || 'Playlist' %>">
                                                    <i class="fas fa-sign-out-alt mr-2"></i> Leave Playlist
                                                </button>
                                            </form>
                                        <% } %>
                                    </div>
                                </div>

                                <a href="/playlist/<%= playlist._id %>">
                                    <div class="flex items-start h-full">
                                        <div class="flex items-start space-x-4 flex-1 min-w-0">
                                            <div
                                                class="w-16 h-16 bg-black/30 rounded-lg flex items-center justify-center flex-shrink-0">
                                                <i class="fas fa-users text-xl text-white/80"></i>
                                            </div>
                                            <div class="flex-1 min-w-0">
                                                <h3 class="text-white font-bold text-lg truncate">
                                                    <%= playlist.name || 'Playlist' %>
                                                </h3>
                                                <!-- Empty space to maintain height -->
                                                <div class="mt-4">
                                                    <p class="text-gray-400 text-sm truncate">
                                                        <i class="fas fa-share-alt mr-1"></i>
                                                        Shared by: <%= playlist.ownerName || 'Friend' %>
                                                    </p>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div
                            class="text-center py-12 border-2 border-dashed border-gray-700 rounded-xl">
                            <i class="fas fa-users text-4xl text-gray-600 mb-4"></i>
                            <p class="text-gray-400">No shared playlists yet</p>
                            <p class="text-gray-500 text-sm mt-2">
                                <i class="fas fa-plus-circle mr-1"></i>
                                Create a playlist or join one using a code
                            </p>
                        </div>
                    <% } %>
                </div>
            </section>

            <!-- Recently Active Section -->
            <section class="py-4">
                <div class="mb-3">
                    <h2 class="text-white text-lg font-semibold">
                        <i class="fas fa-clock mr-2"></i>Recently Active
                    </h2>
                </div>
                <div class="grid grid-cols-2 gap-3">
                    <% if (activePlaylists && activePlaylists.length > 0) { %>
                        <% activePlaylists.forEach((playlist, index)=> { %>
                            <div class="bg-gray-900/50 p-3 rounded-lg border border-gray-800 playlist-card">
                                <a href="/playlist/<%= playlist._id %>">
                                    <div class="flex items-center space-x-3">
                                        <div class="w-12 h-12 bg-purple-500 rounded-lg 
                                                flex items-center justify-center flex-shrink-0">
                                            <i class="fas fa-fire text-white text-sm"></i>
                                        </div>
                                        <div class="flex-1 min-w-0">
                                            <h4 class="text-white font-medium text-sm truncate">
                                                <%= playlist.name || 'Playlist' %>
                                            </h4>
                                            <div class="flex items-center space-x-3 mt-1">
                                                <span class="text-gray-400 text-xs">
                                                    <%= playlist.members ? playlist.members.length : 1 %>
                                                </span>
                                                <span class="text-gray-400 text-xs">
                                                    <%= playlist.songs ? playlist.songs.length : 0 %>
                                                </span>
                                            </div>
                                            <p class="text-gray-500 text-xs mt-1">
                                                <i class="fas fa-history mr-1"></i>
                                                Updated <%= playlist.lastActive || 'recently' %>
                                            </p>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="col-span-2 text-center py-4 text-gray-500">
                            <i class="fas fa-inbox text-lg mb-2"></i>
                            <p class="text-sm">No recently active playlists</p>
                        </div>
                    <% } %>
                </div>
            </section>
        <% } %>
    </main>

    <!-- Bottom Navigation -->
    <nav class="fixed bottom-0 left-0 right-0 w-full bg-black/90 backdrop-blur-lg border-t border-gray-800 z-50">
        <div class="flex justify-around py-3">
            <a href="/" class="text-gray-400 flex flex-col items-center">
                <i class="fas fa-home text-lg"></i>
                <span class="text-xs mt-1">Home</span>
            </a>
            <a href="/explore" class="text-gray-400 flex flex-col items-center">
                <i class="fas fa-compass text-lg"></i>
                <span class="text-xs mt-1">Explore</span>
            </a>
            <a href="/library" class="text-gray-400 flex flex-col items-center">
                <i class="fas fa-heart text-lg"></i>
                <span class="text-xs mt-1">Library</span>
            </a>
            <a href="/profile" class="text-gray-400 flex flex-col items-center">
                <i class="fas fa-user text-lg"></i>
                <span class="text-xs mt-1">Profile</span>
            </a>
        </div>
    </nav>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Info Modal functionality
            const infoBtn = document.getElementById('infoBtn');
            const infoModal = document.getElementById('infoModal');
            const infoModalOverlay = document.getElementById('infoModalOverlay');
            const closeInfoModal = document.getElementById('closeInfoModal');

            // Create Modal functionality
            const createBtn = document.getElementById('createPlaylistBtn');
            const createModal = document.getElementById('createModal');
            const createModalOverlay = document.getElementById('createModalOverlay');
            const closeCreateModal = document.getElementById('closeCreateModal');

            // Delete Modal functionality
            const deleteConfirmModal = document.getElementById('deleteConfirmModal');
            const deleteConfirmModalOverlay = document.getElementById('deleteConfirmModalOverlay');
            const closeDeleteModal = document.getElementById('closeDeleteModal');
            const cancelDeleteBtn = document.getElementById('cancelDeleteBtn');
            const confirmDeleteBtn = document.getElementById('confirmDeleteBtn');

            // Leave Modal functionality
            const leaveConfirmModal = document.getElementById('leaveConfirmModal');
            const leaveConfirmModalOverlay = document.getElementById('leaveConfirmModalOverlay');
            const closeLeaveModal = document.getElementById('closeLeaveModal');
            const cancelLeaveBtn = document.getElementById('cancelLeaveBtn');
            const confirmLeaveBtn = document.getElementById('confirmLeaveBtn');

            // Join Form
            const joinForm = document.getElementById('joinPlaylistForm');
            const playlistCodeInput = document.getElementById('playlistCode');

            // Current playlist to delete/leave
            let currentPlaylistToDelete = null;
            let currentPlaylistToLeave = null;

            // --- INFO MODAL ---
            // Open info modal
            infoBtn.addEventListener('click', function () {
                infoModal.style.display = 'block';
                infoModalOverlay.style.display = 'block';
            });

            // Close info modal on X button
            closeInfoModal.addEventListener('click', function () {
                infoModal.style.display = 'none';
                infoModalOverlay.style.display = 'none';
            });

            // Close info modal on overlay click
            infoModalOverlay.addEventListener('click', function () {
                infoModal.style.display = 'none';
                infoModalOverlay.style.display = 'none';
            });

            // --- CREATE MODAL ---
            // Open create modal
            createBtn.addEventListener('click', function () {
                createModal.style.display = 'block';
                createModalOverlay.style.display = 'block';
            });

            // Close create modal on X button
            closeCreateModal.addEventListener('click', function () {
                createModal.style.display = 'none';
                createModalOverlay.style.display = 'none';
            });

            // Close create modal on overlay click
            createModalOverlay.addEventListener('click', function () {
                createModal.style.display = 'none';
                createModalOverlay.style.display = 'none';
            });

            // --- DELETE MODAL ---
            // Function to open delete confirmation modal
            function openDeleteModal(playlistId, playlistName) {
                currentPlaylistToDelete = playlistId;
                // Update modal text with playlist name
                document.querySelector('#deleteConfirmModal p').innerHTML = 
                    `Are you sure you want to delete <strong>"${playlistName}"</strong>? This action cannot be undone.`;
                deleteConfirmModal.style.display = 'block';
                deleteConfirmModalOverlay.style.display = 'block';
            }

            // Close delete modal on X button
            closeDeleteModal.addEventListener('click', function () {
                deleteConfirmModal.style.display = 'none';
                deleteConfirmModalOverlay.style.display = 'none';
                currentPlaylistToDelete = null;
            });

            // Close delete modal on cancel button
            cancelDeleteBtn.addEventListener('click', function () {
                deleteConfirmModal.style.display = 'none';
                deleteConfirmModalOverlay.style.display = 'none';
                currentPlaylistToDelete = null;
            });

            // Close delete modal on overlay click
            deleteConfirmModalOverlay.addEventListener('click', function () {
                deleteConfirmModal.style.display = 'none';
                deleteConfirmModalOverlay.style.display = 'none';
                currentPlaylistToDelete = null;
            });

            // Handle delete confirmation
            confirmDeleteBtn.addEventListener('click', function () {
                if (currentPlaylistToDelete) {
                    const form = document.getElementById(`delete-form-${currentPlaylistToDelete}`);
                    if (form) {
                        form.submit();
                    }
                }
            });

            // --- LEAVE MODAL ---
            // Function to open leave confirmation modal
            function openLeaveModal(playlistId, playlistName) {
                currentPlaylistToLeave = playlistId;
                // Update modal text with playlist name
                document.querySelector('#leaveConfirmModal p').innerHTML = 
                    `Are you sure you want to leave <strong>"${playlistName}"</strong>? You won't be able to access it unless you rejoin with the code.`;
                leaveConfirmModal.style.display = 'block';
                leaveConfirmModalOverlay.style.display = 'block';
            }

            // Close leave modal on X button
            closeLeaveModal.addEventListener('click', function () {
                leaveConfirmModal.style.display = 'none';
                leaveConfirmModalOverlay.style.display = 'none';
                currentPlaylistToLeave = null;
            });

            // Close leave modal on cancel button
            cancelLeaveBtn.addEventListener('click', function () {
                leaveConfirmModal.style.display = 'none';
                leaveConfirmModalOverlay.style.display = 'none';
                currentPlaylistToLeave = null;
            });

            // Close leave modal on overlay click
            leaveConfirmModalOverlay.addEventListener('click', function () {
                leaveConfirmModal.style.display = 'none';
                leaveConfirmModalOverlay.style.display = 'none';
                currentPlaylistToLeave = null;
            });

            // Handle leave confirmation
            confirmLeaveBtn.addEventListener('click', function () {
                if (currentPlaylistToLeave) {
                    const form = document.getElementById(`leave-form-${currentPlaylistToLeave}`);
                    if (form) {
                        form.submit();
                    }
                }
            });

            // --- DELETE BUTTON HANDLERS ---
            document.querySelectorAll('.delete-playlist-btn').forEach(button => {
                button.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    const playlistId = this.getAttribute('data-playlist-id');
                    const playlistName = this.getAttribute('data-playlist-name');
                    
                    openDeleteModal(playlistId, playlistName);
                });
            });

            // --- LEAVE BUTTON HANDLERS ---
            document.querySelectorAll('.leave-playlist-btn').forEach(button => {
                button.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    const playlistId = this.getAttribute('data-playlist-id');
                    const playlistName = this.getAttribute('data-playlist-name');
                    
                    openLeaveModal(playlistId, playlistName);
                });
            });

            // --- CREATE FORM SUBMISSION ---
            const createForm = document.getElementById('createPlaylistForm');
            if (createForm) {
                createForm.addEventListener('submit', function (e) {
                    e.preventDefault();

                    const playlistName = document.getElementById('playlistName').value.trim();

                    if (!playlistName) {
                        alert('Please enter a playlist name');
                        return;
                    }

                    // Submit the form
                    this.submit();

                    // Close modal after submission
                    setTimeout(() => {
                        createModal.style.display = 'none';
                        createModalOverlay.style.display = 'none';
                    }, 100);
                });
            }

            // --- JOIN FORM ---
            if (joinForm && playlistCodeInput) {
                joinForm.addEventListener('submit', function (e) {
                    e.preventDefault();

                    const code = playlistCodeInput.value.trim();

                    // Validate 9-digit code
                    if (!code || code.length !== 9) {
                        alert('Please enter a valid 9-digit code');
                        playlistCodeInput.focus();
                        return;
                    }

                    // Check if it's a valid number
                    if (isNaN(code) || parseInt(code) < 100000000 || parseInt(code) > 999999999) {
                        alert('Please enter a valid 9-digit code between 100000000 and 999999999');
                        playlistCodeInput.focus();
                        return;
                    }

                    // Redirect to join endpoint with code as parameter
                    window.location.href = `/join/shared/playlist/${code}`;
                });

                // Prevent typing non-numeric characters
                playlistCodeInput.addEventListener('keypress', function (e) {
                    // Allow: backspace, delete, tab, escape, enter
                    if ([46, 8, 9, 27, 13].indexOf(e.keyCode) !== -1 ||
                        // Allow: Ctrl+A, Ctrl+C, Ctrl+V, Ctrl+X
                        (e.keyCode === 65 && e.ctrlKey === true) ||
                        (e.keyCode === 67 && e.ctrlKey === true) ||
                        (e.keyCode === 86 && e.ctrlKey === true) ||
                        (e.keyCode === 88 && e.ctrlKey === true) ||
                        // Allow: home, end, left, right
                        (e.keyCode >= 35 && e.keyCode <= 39)) {
                        return;
                    }

                    // Ensure that it is a number and stop the keypress
                    if ((e.shiftKey || (e.keyCode < 48 || e.keyCode > 57)) && (e.keyCode < 96 || e.keyCode > 105)) {
                        e.preventDefault();
                    }
                });

                // Limit to 9 digits
                playlistCodeInput.addEventListener('input', function () {
                    if (this.value.length > 9) {
                        this.value = this.value.slice(0, 9);
                    }
                });
            }

            // Menu icon functionality
            let activeDropdown = null;

            document.querySelectorAll('.menu-icon').forEach(icon => {
                icon.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const index = this.getAttribute('data-playlist-index');
                    const dropdown = document.getElementById(`info-dropdown-${index}`);

                    // Close any other open dropdowns
                    if (activeDropdown && activeDropdown !== dropdown) {
                        activeDropdown.classList.remove('show');
                    }

                    // Toggle current dropdown
                    if (dropdown.classList.contains('show')) {
                        dropdown.classList.remove('show');
                        activeDropdown = null;
                    } else {
                        dropdown.classList.add('show');
                        activeDropdown = dropdown;
                    }
                });
            });
            
            // Close dropdown when clicking outside
            document.addEventListener('click', function (e) {
                if (activeDropdown && !activeDropdown.contains(e.target) &&
                    !e.target.closest('.menu-icon')) {
                    activeDropdown.classList.remove('show');
                    activeDropdown = null;
                }
            });

            // Copy share code from dropdown
            document.querySelectorAll('.share-code-dropdown').forEach(codeElement => {
                codeElement.addEventListener('click', function (e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const code = this.getAttribute('data-code');
                    if (code !== 'N/A') {
                        navigator.clipboard.writeText(code).then(() => {
                            const originalText = this.textContent;
                            this.textContent = 'Copied!';
                            this.style.color = '#10B981';

                            setTimeout(() => {
                                this.textContent = originalText;
                                this.style.color = '';
                            }, 1500);
                        });
                    }
                });
            });

            // Close modals on ESC key
            document.addEventListener('keydown', function (e) {
                if (e.key === 'Escape') {
                    if (infoModal.style.display === 'block') {
                        infoModal.style.display = 'none';
                        infoModalOverlay.style.display = 'none';
                    }
                    if (createModal.style.display === 'block') {
                        createModal.style.display = 'none';
                        createModalOverlay.style.display = 'none';
                    }
                    if (deleteConfirmModal.style.display === 'block') {
                        deleteConfirmModal.style.display = 'none';
                        deleteConfirmModalOverlay.style.display = 'none';
                        currentPlaylistToDelete = null;
                    }
                    if (leaveConfirmModal.style.display === 'block') {
                        leaveConfirmModal.style.display = 'none';
                        leaveConfirmModalOverlay.style.display = 'none';
                        currentPlaylistToLeave = null;
                    }

                    // Close dropdown on ESC
                    if (activeDropdown) {
                        activeDropdown.classList.remove('show');
                        activeDropdown = null;
                    }
                }
            });
        });
    </script>
</body>
</html>